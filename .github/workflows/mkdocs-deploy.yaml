name: Publish to GitHub Pages
on:
  push:
    branches: [ "master" ]
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
    - name: Check out repo
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: 3.10
    - name: Install MkDocs
      run: |
        python -m pip install --upgrade pip
        python -m pip install mkdocs==1.3.1
    - name: Build pages
      run: mkdocs build
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v5
      with:
        path: site/
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v5

